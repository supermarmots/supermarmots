function solution(numbers, num1, num2) {
    return numbers.slice(num1, num2 + 1)
    // 테스트 1만 통과한 내 풀이
    // var answer = [];
    // for(let i=num1; numbers[i] <= num2; i ++) {
    //     answer = numbers.slice(num1, num2 +1)
    // }
    // return answer;
}